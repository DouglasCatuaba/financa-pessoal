// PostCSS configuration for the Finança Pessoal app.
// This configuration enables Tailwind CSS and Autoprefixer to process
// your styles when running the build and dev scripts. The file uses
// the CommonJS format so it can be consumed by Vite without any extra
// configuration.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};