import React from 'react';

/**
 * Placeholder component for rules. Automation rules will allow you to
 * categorize transactions automatically based on their description or
 * other fields. Not implemented in the first version.
 */
const Rules: React.FC = () => {
  return <p>Funcionalidade de regras em desenvolvimento.</p>;
};

export default Rules;