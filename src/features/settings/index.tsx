import React from 'react';

/**
 * Placeholder component for settings. This page will contain options
 * such as backup/restore, export/import and theme toggles. Not
 * implemented in the first version.
 */
const Settings: React.FC = () => {
  return <p>Configurações em desenvolvimento.</p>;
};

export default Settings;